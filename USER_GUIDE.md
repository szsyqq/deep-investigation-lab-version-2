# Deep Investigation Lab V2 操作手册

## 1. 以后怎么使用

最简单的方式是在 Codex 中打开本仓库，直接提出研究请求：

- 主体研究：“调查空客为什么订单创新高，但交付和现金流承压。”
- 话题研究：“调查低空经济热潮背后的需求、政策、参与者和风险。”

系统会依次完成：

1. 识别 `entity` 或 `topic`；
2. 生成问题树与研究范围；
3. 建立证据账本；
4. 提炼主张与反证；
5. 规划视觉表达；
6. 生成固定框架下的报告；
7. 校验并发布。

修改现有报告时，应说明问题与目的。例如：“新材料改变了供应链章节的结论，请先判断是否需要重排结构，再修改正文。”系统会将修改分为证据更新、局部重写、结构调整或全文重写。

## 2. 怎么维护

- 报告内容：维护 `content/reports/<slug>/`，每篇报告彼此隔离。
- 研究规则：维护 `.codex/skills/deep-investigation/` 和 `docs/`。仅当多个案例重复出现同一问题时再升级规则。
- 网站框架：维护 `app/`。不要为单篇报告复制页面框架。

人工接受、修改或拒绝 AI 建议后，把决定写入该报告的 `editorial-state.json`。这才是系统以后“越用越懂”的依据。

## 3. 指令和功能

```bash
# 创建研究包
npm run research:new -- "你的研究请求"

# 校验研究包
npm run research:validate

# 运行自动测试
npm test

# 本地预览
npm run dev

# 生产构建
npm run build
```

当前功能包括研究分类、证据账本、主张地图、视觉规划、修改分级、报告隔离和固定网站发布。

## 4. 后续开发

优先顺序：

1. 让 `publication.json` 自动渲染为正式报告；
2. 增加真实证据采集与来源冲突提示；
3. 建立可操作的编辑决策面板；
4. 扩展关系网、分布、地理、情景和风险表达模式；
5. 增加报告状态、审批、版本比较、发布与撤回后台；
6. 完整跑通一篇真实新报告后，再考虑迁移 V1 旧报告。
